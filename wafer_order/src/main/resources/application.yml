server:
  port: 8081
spring:
  application:
    name: wafer-order #设置当前应用名称，在eureka中Aoolication显示，通过该名称可以获取路径
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db2024?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&tinyInt1isBit=false
    username: root
    password: 19991201
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #配置nacos地址
      config:
        import-check:
          enabled: false
